name: flutter_application_1
description: "SHAQATI - Real Estate Rental App 🏠"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0  # تم ضبط النسخة لتكون مستقرة ومتوافقة
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  fl_chart: ^0.66.0
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  provider: ^6.1.1
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  # ✅ (1) تمت إضافة مكتبة الأيقونات هنا
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  # ✅ (2) الأصول والصور
  assets:
    - assets/images/SHAQATI_LOGIN.png
    - assets/images/hero_image.png
    - assets/images/icon.png # تأكد أنك وضعت صورة الشعار هنا بهذا الاسم

# ✅ (3) إعدادات توليد الأيقونة
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/icon.png"
  min_sdk_android: 21