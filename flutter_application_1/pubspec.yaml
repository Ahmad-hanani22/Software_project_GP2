name: flutter_application_1
description: "SHAQATI - Real Estate Rental App 🏠"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # UI Icons
  cupertino_icons: ^1.0.8

  # Networking & Storage
  http: ^1.2.0
  shared_preferences: ^2.2.2

  # Utilities
  intl: ^0.20.2
  provider: ^6.1.1
  image_picker: ^1.0.7
  csv: ^6.0.0

  # 🎤 Voice Recognition & Speech
  speech_to_text: ^7.3.0
  flutter_tts: ^4.0.2
  record: ^5.2.1  # Audio recording (updated to latest compatible version)
  audioplayers: ^5.2.1  # Audio playback

  # Links & external apps
  url_launcher: ^6.3.0

  # 🔔 Firebase & Notifications
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^18.0.1

  # Maps & Charts
  fl_chart: ^0.66.0
  flutter_map: ^6.0.0
  google_maps_flutter: ^2.6.0
  latlong2: ^0.9.0
  geolocator: ^12.0.0

  # Image Gallery & Carousel
  carousel_slider: ^5.0.0
  photo_view: ^0.14.0
  cached_network_image: ^3.3.1

  # 3D Viewer
  model_viewer_plus: ^1.7.0

  # 📄 PDF, Printing & File Management
  pdf: ^3.10.6
  printing: ^5.12.0
  path_provider: ^2.0.15
  open_file: ^3.3.2
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1
  google_maps_flutter_web: ^0.5.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/SHAQATI_LOGIN.png
    - assets/images/hero_image.png
    - assets/images/icon.png
    - assets/images/buyers_guide.jpg
    - assets/images/market_trends.jpg
    - assets/images/promotional_banner.jpg

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/icon.png"
  min_sdk_android: 21

# Fix record_linux compatibility issue
dependency_overrides:
  record_linux: ^1.2.1
