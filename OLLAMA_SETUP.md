# 🆓 إعداد Ollama (Local LLM) - مجاني تماماً

## 📋 نظرة عامة

تم تحويل نظام AI من DeepSeek إلى **Ollama** ليكون مجاني تماماً ويعمل محلياً على جهازك.

## 🛠️ خطوات التثبيت

### 1️⃣ تثبيت Ollama

#### Windows (PowerShell):
```powershell
winget install ollama
```

أو من الموقع الرسمي:
- اذهب إلى: https://ollama.com/download
- حمّل الملف المناسب لنظامك

#### macOS (Homebrew):
```bash
brew install ollama
```

#### Linux:
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

### 2️⃣ تشغيل Ollama

افتح Terminal/PowerShell واكتب:
```bash
ollama serve
```

**⚠️ مهم:** اترك هذا Terminal مفتوحاً - Ollama يجب أن يبقى يعمل.

### 3️⃣ تحميل موديل مجاني

افتح Terminal جديد واكتب:

```bash
# للموديلات الخفيفة (موصى به للبداية)
ollama pull llama2

# أو Mistral (أفضل جودة)
ollama pull mistral

# أو Mixtral (أفضل جودة لكن يحتاج GPU قوي)
ollama pull mixtral
```

**ملاحظة:** 
- `llama2` يحتاج ~4GB RAM
- `mistral` يحتاج ~5GB RAM  
- `mixtral` يحتاج GPU قوي

### 4️⃣ التحقق من التثبيت

```bash
# عرض الموديلات المثبتة
ollama list

# اختبار الموديل
ollama run llama2 "مرحباً"
```

## ⚙️ إعدادات Backend

### 1️⃣ إضافة متغيرات البيئة (اختياري)

في ملف `backend/.env`:

```env
# Ollama Configuration (اختياري)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama2
```

**ملاحظة:** إذا لم تضيف هذه المتغيرات، سيستخدم النظام القيم الافتراضية:
- `OLLAMA_BASE_URL`: `http://localhost:11434`
- `OLLAMA_MODEL`: `llama2`

### 2️⃣ تثبيت Dependencies

```bash
cd backend
npm install
```

## ✅ اختبار النظام

### 1️⃣ فحص حالة Ollama

```bash
# من Backend
curl http://localhost:3000/api/ai/health
```

يجب أن ترى:
```json
{
  "success": true,
  "health": {
    "ollamaAvailable": true,
    "targetModel": "llama2",
    "hasTargetModel": true,
    "provider": "Ollama (Local LLM)",
    "status": "ready"
  }
}
```

### 2️⃣ اختبار AI Assistant

1. شغّل Backend:
   ```bash
   cd backend
   npm start
   ```

2. افتح Flutter App واذهب إلى AI Assistant

3. اسأل سؤال مثل: "حلل بنية مشروع SHAQATI"

## 🔧 استكشاف الأخطاء

### ❌ خطأ: "Cannot connect to Ollama server"

**الحل:**
1. تأكد أن Ollama يعمل:
   ```bash
   ollama serve
   ```

2. تحقق من العنوان:
   ```bash
   curl http://localhost:11434/api/tags
   ```

### ❌ خطأ: "Model not found"

**الحل:**
```bash
# حمّل الموديل
ollama pull llama2
```

### ❌ خطأ: "Connection timeout"

**الحل:**
- الموديلات الكبيرة (مثل mixtral) قد تأخذ وقتاً طويلاً
- استخدم موديل أصغر مثل `llama2` أو `mistral`

## 📊 مقارنة الموديلات

| الموديل | الحجم | RAM المطلوب | الجودة | السرعة |
|---------|-------|-------------|--------|--------|
| llama2 | 3.8GB | ~4GB | ⭐⭐⭐⭐ | ⚡⚡⚡ |
| mistral | 4.1GB | ~5GB | ⭐⭐⭐⭐⭐ | ⚡⚡⚡ |
| mixtral | 26GB | GPU قوي | ⭐⭐⭐⭐⭐ | ⚡⚡ |

## 🎯 نصائح

1. **للبداية:** استخدم `llama2` - سريع وخفيف
2. **للجودة:** استخدم `mistral` - أفضل توازن
3. **للأداء:** إذا كان عندك GPU قوي، استخدم `mixtral`

## 📝 ملاحظات مهمة

- ✅ **مجاني تماماً** - لا يحتاج API keys
- ✅ **يعمل محلياً** - لا يحتاج إنترنت بعد التثبيت
- ✅ **خصوصية كاملة** - البيانات لا تخرج من جهازك
- ⚠️ **أبطأ من السحابي** - لكن مجاني تماماً
- ⚠️ **يحتاج RAM** - تأكد أن جهازك لديه ذاكرة كافية

## 🚀 جاهز!

الآن نظام AI يعمل محلياً ومجاني تماماً! 🎉
