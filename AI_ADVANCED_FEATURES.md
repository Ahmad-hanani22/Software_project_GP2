# 🚀 AI Assistant - ميزات متقدمة

## ✅ ما تم تطبيقه

### 1️⃣ Tool Calling (أدوات ذكية)
الـ AI يمكنه الآن استدعاء أدوات حقيقية من قاعدة البيانات:

#### الأدوات المتاحة:
- **getTopViewedProperties**: أكثر العقارات مشاهدة
- **getRecommendedProperties**: اقتراحات بناءً على الميزانية والمدينة
- **checkAvailability**: فحص توفر عقار
- **calculateRentEstimate**: حساب تقدير الإيجار
- **getPropertyStats**: إحصائيات عقار
- **getUserPreferences**: تفضيلات المستخدم

**مثال:**
```
المستخدم: "اقترح شقق في رام الله بميزانية 500$"
→ AI يستدعي getRecommendedProperties(userId, 500, "رام الله")
→ يعيد نتائج حقيقية من DB
→ يشرح سبب الاقتراح (Explainable AI)
```

---

### 2️⃣ Role-Aware AI (ذكاء حسب الدور)
System Prompt يتغير حسب دور المستخدم:

#### Tenant (مستأجر):
- اقتراح عقارات مناسبة
- شرح نظام العقود
- مساعدة في طلبات الصيانة

#### Landlord (مالك):
- تحليل أداء العقارات
- اقتراح تسعير ذكي
- تحليل السوق

#### Admin (إدمن):
- مراقبة النظام
- تحليل الإحصائيات
- كشف أنماط غير طبيعية

---

### 3️⃣ Session Memory (ذاكرة الجلسة)
AI يتذكر آخر 5 أسئلة في نفس الجلسة:

```dart
final List<String> _sessionHistory = [];
// يحفظ آخر 5 أسئلة
```

**الفائدة:**
- "لسه كنا نحكي عن شقق رام الله..."
- أزرار ذكية تتغير حسب السياق

---

### 4️⃣ Dynamic Smart Buttons (أزرار ذكية ديناميكية)
الأزرار تتغير حسب المحادثة:

- إذا سأل عن **عقارات** → أزرار: "اقترح عقارات", "أرخص", "قريب من الجامعة"
- إذا سأل عن **عقود** → أزرار: "كيف أبدأ عقد", "حساب الإيجار"
- إذا سأل عن **ميزات** → أزرار: "ما هي الميزات", "كيف أستخدم الصيانة"

---

### 5️⃣ Explainable AI (AI يشرح قراراته)
AI يشرح سبب الاقتراح:

```
"اقترحت هذا العقار لأن:
1. السعر ($500) ضمن ميزانيتك
2. في المدينة المفضلة لديك
3. متاح للإيجار فوراً"
```

---

## 📁 الملفات الجديدة/المعدلة

### Backend:
1. **`backend/utils/aiTools.js`** (جديد)
   - جميع AI Tools Functions
   - Mapping للـ Tools

2. **`backend/controllers/aiController.js`** (محدّث)
   - Tool Calling Logic
   - Role-Aware System Prompt
   - Function Definitions

### Flutter:
3. **`flutter_application_1/lib/screens/ai_assistant_screen.dart`** (محدّث)
   - Session Memory
   - Dynamic Smart Buttons
   - UI محسّن

---

## 🔧 كيفية الاستخدام

### مثال 1: استدعاء Tool
```
المستخدم: "أعطني أكثر العقارات مشاهدة"
→ AI يستدعي: getTopViewedProperties(userId, 5)
→ يرجع قائمة حقيقية
```

### مثال 2: Role-Aware
```
Tenant يسأل: "اقترح عقارات"
→ System Prompt يركز على اقتراحات العقارات
→ AI يستخدم getRecommendedProperties
```

### مثال 3: Dynamic Buttons
```
بعد سؤال عن العقارات:
→ الأزرار تتغير تلقائياً:
  • "اقترح عقارات ضمن ميزانيتي"
  • "أرخص من اللي شفته"
```

---

## 🎯 الخطوات التالية المقترحة

### 1. Rules Engine (ذكاء بدون تكلفة)
```javascript
// قواعد بسيطة قبل AI:
if (price > budget) → لا تعرض
if (visits > 100) → اعرض أولاً
```

### 2. Long-term Memory
- حفظ تفضيلات المستخدم في DB
- AI "يعرفك" مع الوقت

### 3. Voice Assistant
- استخدام Speech-to-Text
- AI يجاوب صوتياً

### 4. Analytics AI
- Dashboard ذكي للأدمن
- كشف أنماط غير طبيعية

---

## ✅ الاختبار

### اختبار Tool Calling:
```bash
# في Flutter
"اقترح عقارات في رام الله بميزانية 500$"
```

### اختبار Role-Aware:
```bash
# تسجيل دخول كـ Tenant vs Landlord
# نفس السؤال → رد مختلف
```

### اختبار Memory:
```bash
# سؤال 1: "شقق رام الله"
# سؤال 2: "أرخص منها"
# → AI يفهم "أرخص منها" = أرخص من شقق رام الله
```

---

## 📊 الأداء

- **Tool Calling**: +2-3 ثوانٍ (استعلام DB)
- **Memory**: بدون تأثير (محلي في Flutter)
- **Role-Aware**: بدون تأثير (System Prompt فقط)

---

## 🔒 الأمان

✅ جميع Tools محمية بـ Authentication  
✅ userId يتم تمريره تلقائياً من Token  
✅ لا يمكن الوصول لبيانات مستخدمين آخرين  

---

## 💡 ملاحظات

- **Tool Calling** يعمل مع File Search معاً
- **Session Memory** يتم مسحه عند إغلاق التطبيق
- **Dynamic Buttons** تعتمد على آخر 5 أسئلة فقط

---

**🎉 النظام جاهز للاستخدام!**
